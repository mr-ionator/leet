class Solution:
    def buildArray(self, target: List[int], n: int) -> List[str]:
        output = []
        ptr = 0
        for i in range(1,n +1):
            if ptr >= len(target):
                    break
            if target[ptr] == i:
                 output.append("Push")
                 ptr +=1
            elif target[ptr] != i:
                output.append("Push")
                output.append("Pop")  
        return output    